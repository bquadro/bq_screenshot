## Прогресс загрузки
- добавил `upload-file` IPC, `UploadController` теперь стримит файл, считает байты и через callback шлёт `upload-progress` с `{uploadId, percent}` (`app/src/classes/UploadController.js:1-44`, `app/src/classes/IpcHandlers.js:20-110`);
- прокинул слушатель `electronAPI.onUploadProgress` в preload (`app/src/preload.js:13-47`), App.vue подписывается на события и обновляет `uploadProgressLabel`, которая передаётся в `HomeActions` (`app/src/App.vue:12-50`, `app/src/components/HomeActions.vue:1-34`);
- после остановки записи видеокнопка перестаёт показывать “в записи” сразу, а прогресс показа значения отображается до завершения выгрузки; добавлены новые переводы `capture.uploadProgress` (ru/en) и вспомогательный хелпер `trackVideoUpload`.
