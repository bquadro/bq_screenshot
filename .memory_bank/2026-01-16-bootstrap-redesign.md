## Переработка интерфейса
- добавил `app/src/config/ui.js` и логотип `app/src/assets/logo.svg`, чтобы заголовок и значок шапки брались из конфигурации
- переписал `App.vue` под новую структуру: хедер с логотипом/заголовком и кнопкой настроек, контент с тремя карточками действий (полный, область, запись) и футер с копирайтом, используя массив `actions` для рендеринга
- расширил локализации `ru/en` новыми ключами (`header.settingsButton`, `actions.*`, `capture.*`, `settings.openingPanel`, `footer.copy`) и подключил Bootstrap, чтобы интерфейс держался на утилитах грида/форм
