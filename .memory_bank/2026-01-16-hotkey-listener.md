## Захват горячих клавиш
- добавил в `App.vue` слушатель `window.keydown`, который активируется после нажатия кнопки «Захватить» и формирует комбинацию клавиш с модификаторами (`Ctrl`, `Alt`, `Shift`, `Meta`)
- при успешной записи комбинации поле `settingsForm` обновляется, статус меняется на `settings.hotkeyCapturedStatus`, и слушатель удаляется, чтобы не оставлять активное значение
- новое поведение описано в `.memory_bank/2026-01-16-hotkey-listener.md`, а компоненты отправляют событие `capture-hotkey` с именем поля
