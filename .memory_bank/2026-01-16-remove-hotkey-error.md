# Устранение ошибки hotkeys 2026-01-16
- Удалил все вызовы к `hotkeys` и функции захвата горячих клавиш из `app/src/App.vue`, поэтому теперь сборка проходит без `ReferenceError: hotkeys is not defined`.
- Оставил единственный обработчик `handleHotkeyCapture`, выдающий сообщение `settings.hotkeysDisabled`, чтобы кнопка старта оставалась активной, но не работала с зависающим кодом.
- В остальном форма и статус настроек остались неизменными, отладка больше не падает из-за отсутствующей библиотеки.
