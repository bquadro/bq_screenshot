## Перенос ядра
- перенёс логику `main.js` из старого рендерера, добавив ESM-совместимые импорты, функции нормализации настроек и обработку `ipc`-событий (включая сохранение скрина)
- переписал `preload.js`, чтобы он выдавал через `contextBridge` API для получения версии, захвата скрина и работы с настройками
- добавил классы `CaptureService`, `SettingsService`, `LocalizationService` и словари `ru`, `en` вместе с `lang/index.js`, чтобы обеспечить инфраструктуру пользовательского интерфейса
