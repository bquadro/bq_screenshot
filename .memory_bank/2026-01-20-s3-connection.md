Контекст:
- Добавить кнопку на странице настроек для проверки подключения к S3/Minio.
- Используется уже существующий UploadController с настройками и загрузкой скриншотов.
Прогресс:
- В UploadController появился метод `checkConnection`, который использует `HeadBucketCommand`, возвращает коды ошибок и логирует неудачи.
- Добавил IPC-обработчик `check-s3-connection`, экспонируем его через preload и вызываем из рендера.
- В App.vue появились `testS3Connection`, состояния `isS3Testing`/`s3TestStatus` и переводимые строки, управление результатом и сообщение об ошибке.
- SettingsForm теперь рендерит кнопку `Проверить подключение`, принимает флаги и текст статуса, отправляет событие в App; интерфейс показывает статус рядом с кнопкой.
Следующие шаги:
- Протестировать кнопку с реальными параметрами Minio (включая отключённые настройки) и убедиться, что сообщения локализуются и отображаются корректно.
