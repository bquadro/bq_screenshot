Контекст:
- Требуется, чтобы действия из контекстного меню трея (скриншот во весь экран, область, запись, настройки) не показывали окно до завершения захвата, как было сделано ранее для глобальных клавиш.
Прогресс:
- Убрал прежнюю последовательность `showWindow` -> `sendAction` в `TrayController.buildMenu`, теперь пункты меню просто отправляют `tray-action`, оставляя за рендерером решение, когда показывать главное окно после успешного скриншота (там уже вызывается `showWindowAfterCapture`).
Следующие шаги:
- Проверить в запущенной сборке, что при выборе пункта меню сначала делается захват, затем окно открывается по `showMainWindow`, и окно не появляется до завершения процесса.
