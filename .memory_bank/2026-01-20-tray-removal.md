Удалил все изменения, связанные со сворачиванием в трей:

- Вернул `app/src/main.js` к исходному варианту: только BrowserWindow, IPC для скриншотов и выбор папки, без Tray, обработчиков minimise/close и дополнительных глобальных переменных.
- Удалил `onTrayAction` из `preload.js` и удалил подписку на события в `App.vue`, чтобы renderer снова работал только с ранее существующими IPC.
- Убрал добавленную иконку `app/src/assets/tray-icon.png` и отменил связанные записи в `.memory_bank`.
- `npm run lint` (скрипт всё ещё выводит «No linting configured», npm пишет предупреждение по версии).
