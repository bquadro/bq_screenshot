# Проверка сборки 2026-01-21

- убедился, что `scripts/build_publish_mac.sh` всё ещё проверяет Node.js 25, работает из `app/` и теперь вызывает `node ./create_dmg.js` (скрипт в той же папке) после `npm run make`;
- посмотрел `app/create_dmg.js`: пути к `appDir`, `out/make`, `app-icon.icns` и имени продукта корректно вычисляются относительно проекта, и скрипт ищет уже собранные `darwin`-директории перед вызовом `electron-installer-dmg`;
- дополнительных команд (тестов) не запускал, потому что задача была чисто инспекционной.
