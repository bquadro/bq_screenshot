Реализовал первый шаг из «Основных функций»: создание скриншота полного экрана.
- Добавил обработчик `ipcMain.handle('save-screenshot')` в `src/main.js`, показывающий диалог сохранения и записывающий PNG в папку пользователя.
- Расширил `src/preload.js` новой функцией `captureFullScreenScreenshot`, захватывающей экран через `desktopCapturer` и отправляющей base64-данные в main.
- Обновил `src/index.html` и `src/renderer.js`: добавил UI с кнопкой «Сделать скриншот», отображением статуса и превью, а также логику вызова API и отображения результатов.
