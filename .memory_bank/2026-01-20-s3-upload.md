Контекст:
- Настроил загрузку скриншотов в S3/Minio и добавил в интерфейс кнопку копирования ссылки.
Прогресс:
- Установил `@aws-sdk/client-s3` и добавил `UploadController` в main-процесс, который читает настройки, пишет объект в Minio и копирует URL в буфер обмена.
- Расширил `IpcHandlers`/`preload`/`UploadService` для нового IPC (`upload-screenshot`), научил `captureService` сохранять в существующий файл, и сохранять/переиспользовать путь при редактировании.
- Обновил `App.vue` и `HomeActions` так, чтобы после захвата снимка снимок сохранялся, загружался в S3 (если включено в настройках), отображалась ссылка и была кнопка копирования, а также обрабатывалась повторная загрузка после редактирования.
Следующие шаги:
- Проверить на реальных настройках Minio, что URL появляется, копируется и повторно заливается после правки.
