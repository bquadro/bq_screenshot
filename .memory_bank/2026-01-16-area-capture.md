# Захват области 2026-01-16
- Добавил IPC `capture-area` в `main.js`, который открывает прозрачное окно на весь экран с выбором области, ждет координаты и затем кропает скриншот через `sharp`, возвращая Base64.
- Расширил `preload.js`/`CaptureService.js`, чтобы можно было вызвать `captureArea()` из рендерера и отправить выбор области в главное окно.
- Интерфейс теперь вызывает `captureService.captureArea()` при клике на кнопку «Скриншот области», показывает соответствующие статусы и передает результат в редактор (`App.vue`), а также добавил переводы для сообщений о выборе области.
