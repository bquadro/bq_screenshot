Перенёс словари переводов в `src/lang`: создал `ru.js`, `en.js` и индекс `index.js`, экспортирующий оба языка, и подключил их из `renderer.js`, чтобы словари можно было расширять отдельно от логики UI.
