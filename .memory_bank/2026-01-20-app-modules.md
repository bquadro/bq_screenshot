Контекст:
- App.vue стал слишком длинным, нужно сгруппировать утилиты по темам и вынести в отдельные модули, чтобы скрипт оставался лёгким.
Прогресс:
- Добавил `modules/settingsUtils.js`, `modules/uploadLink.js`, `modules/captureActions.js` и `modules/s3Utils.js`, из которых App.vue импортирует создание формы, работу с S3 и обработчики захвата.
- App.vue теперь использует `useUploadLink` и `createCaptureActions`, а настройки, payload и хоткеи собираются через функции из settings-модуля; `ensureDomainEndpoint` тоже вынесен.
- Компоненты и шаблон остались прежними, но вся бизнес-логика сосредоточена в модулях.
Следующие шаги:
- Убедиться, что сборка/горячие клавиши и новые модули работают как прежде (ручной запуск приложения).
