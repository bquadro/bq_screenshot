Вернул проект к исходному состоянию после ошибочного изменения трея:

- Перезаписал `app/src/main.js`, `App.vue`, `preload.js` и классы (`SettingsStorage`, `SettingsService`, `LocalizationService`, `CaptureService`) из последнего коммита, чтобы отменить эксперимент с дополнительными обёртками/комментариями.
- Удалил саммари, связанные с треем/IPC (они теперь не соответствуют коду), и запустил `npm run lint` (скрипт остаётся заглушкой).
