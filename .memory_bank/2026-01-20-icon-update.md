Контекст:
- Новая иконка находится в `app/src/assets/srcIcons/2v.svg`, нужно было получить растровую версию и разложить в используемые icon-файлы (главная и трея).
Прогресс:
- С помощью ImageMagick превёл `2v.svg` в PNG 512x512 для `app-icon.png` и в 32x32 для трёх трэй-иконок (`tray-mac.png`, `tray-linux.png`, `tray-windows.png`), сохранив прозрачный фон и новый дизайн на каждой платформе.
- Все три платформа-специфические трэй-файла теперь согласованы по визуальному стилю и размеру.
Следующие шаги:
- Пересобрать приложение/трей, чтобы убедиться, что новые PNG используются при запуске.
