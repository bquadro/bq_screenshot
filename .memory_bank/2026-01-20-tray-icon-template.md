Контекст:
- Новая цветная иконка для трея на macOS отображалась как чёрный прямоугольник из-за `setTemplateImage(true)`; этой опцией Electron обрабатывает изображение как шаблон и теряются цвета.
Прогресс:
- Убрал вызов `setTemplateImage(true)` в `TrayController`, чтобы цветные PNG использовались без наложения монохрома (`app/src/classes/TrayController.js`).
Следующие шаги:
- Перезапустить приложение на macOS и убедиться, что трей-иконка отображается как цветная картинка из `tray-mac.png`.
