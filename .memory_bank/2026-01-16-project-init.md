Создал минимальную npm-конфигурацию, установил Electron как dev-зависимость и обновил скрипт `start` для запуска приложения.
Наготовил структуру Electron-приложения: `src/main.js` с окном, `src/preload.js` с безопасной связью, `src/index.html` как стартовую страницу и `src/renderer.js` для отображения версии.
Дополнительно обновил `.gitignore` чтобы исключать `node_modules` и лог npm.
