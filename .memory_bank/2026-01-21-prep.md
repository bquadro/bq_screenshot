## Подготовка к работе
- изучил структуру проекта, включая `app/src` и главные классы, уточнил требования (Node.js 25 через `nvm use stable`, хранение скриншотов в `app/src/window`);
- прочитал документацию Electron 40.0.0 (введение и модель процессов) и отметил ключевые моменты: main/renderer/прелоад, контекстная изоляция и IPC;
- подготовил обзор точек входа (`main.js`, `preload.js`, `IpcHandlers`) и подчеркнул зоны внимания (например, отсутствующая папка `app/src/window` и возможность ошибки при записи видео).
