Перерисовал иконку трея:

- Новый `app/src/assets/tray-icon-bs.png` создаёт красный квадрат с чёрными буквами BS, полученный скриптом (32×32, без внешних зависимостей).
- `TrayController` по-прежнему цепляется за этот файл и теперь в сборке (с копированием assets) всегда получит нужную графику, вместо предыдущего чёрного прямоугольника.
