# DMG build fix 2026-01-21

- добавил в `app/create_dmg.js` распаковку ZIP-архивов из `out/make/zip` через `unzip`, чтобы скрипт работал даже без готовой директории `darwin/...`;
- по-прежнему пытаемся извлечь `.app` напрямую, но теперь fallback использует ZIP и запускает `electron-installer-dmg` на распакованных бандлах;
- `scripts/build_publish_mac.sh` остаётся без изменений, он вызывает `node ./create_dmg.js` из `app`, и теперь путь к скрипту должен находиться в `app`.
