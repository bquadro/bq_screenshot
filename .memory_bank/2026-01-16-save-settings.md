# Сохранение настроек 2026-01-16
- Выделил работу с настройками в класс `SettingsStorage` в `app/src/classes`, который умеет собирать дефолт, сливать с пользовательскими значениями, и читать/писать JSON в `app.getPath('userData')`.
- Обновил `app/src/main.js`, чтобы `ipcMain` обращался к `SettingsStorage` для загрузки/сохранения, переместив логику файла в отдельный класс.
- Теперь настройки действительно пишутся в JSON-файл с пути пользователя и будут загружаться при следующем запуске приложения через уже существующий IPC API.
