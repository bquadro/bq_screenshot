Доработал скрытие приложения из дока:

- Убрал ограничение по платформе в обработчике `close` (`app/src/main.js`), чтобы Mac не выходил из приложения и мог скрывать окно в трей.
- `showWindow` теперь явно показывает док (`TrayController.showDock`), а `minimize`/`close` вызывают `hideDock`, чтобы иконка исчезала из панелей и оставалась только в трее.
- `npm run lint` (скрипт по-прежнему «No linting configured»).
