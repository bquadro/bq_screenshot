Контекст:
- Интерфейс должны сделать более компактным: убрать название/описание, перенести кнопки действий в шапку и сделать их иконками.
Прогресс:
- Полностью переработал `AppHeader`: теперь он рисует только логотип, иконки действий (камера, квадрат, запись) и кнопки настроек/назад без подписей; отображает статус справа (`app/src/components/AppHeader.vue`).
- Добавил событие `run-action`, а `App.vue` передаёт массив действий с шаблонами и логику обработчиков (`App.vue` обновлён). Кнопки для действий больше не рендерятся в `HomeActions`, компонент теперь показывает только статус, превью и ссылку (`HomeActions.vue`).
Следующие шаги:
- Проверь в UI: теперь заголовка и описания нет, кнопки видны в шапке как иконки, а остальной контент продолжается ниже.
