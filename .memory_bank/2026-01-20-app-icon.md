Назначил `app-icon.png` основной иконкой приложения:

- Добавил `path.join(__dirname, 'src', 'assets', 'app-icon')` в `packagerConfig.icon` (`app/forge.config.js`), чтобы electron-forge подхватывал наш новый рисунок при упаковке для всех платформ.
- Импортировал `node:path` в `forge.config.js`, чтобы правильно формировать путь.
- `npm run lint` (по-прежнему выводится «No linting configured», npm напоминает про несовместимую версию).
