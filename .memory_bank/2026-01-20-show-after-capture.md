Контекст:
- Требуется показывать окно только после того, как скриншот сделан (чтобы не попадать в кадр при захвате).
Прогресс:
- Убрал прежнюю попытку разворачивать окно в `GlobalHotkeyController` и теперь просто отправляю события.
- Добавил IPC-метод `show-main-window` и экспонирую его в `preload`, чтобы рендерер мог запросить показ окна.
- После успешной загрузки изображения с экрана (`captureFullScreen`, `captureArea`) вызываю `showMainWindow`, чтобы окно появлялось только после завершения захвата.
Следующие шаги:
- Запустить приложение, проверить, что после глобальных клавиш окно появляется только после захвата и действия выполняются.
