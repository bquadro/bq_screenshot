## Повторная подготовка
- перечитал `AGENTS.md` и отметил обновлённую структуру (исходники в `app/src`, классы в `app/src/classes`, переводы — в `app/src/lang`, компоненты Vue в `app/src/components`)
- зафиксировал, что `app` пока содержит стандартный шаблон Vite/Electron и что рабочий код из `src_old` включает классы захвата, локализации, настроек и шаблон с формой
- учёл, что в будущем придётся переписывать/интегрировать содержимое `src_old` в новую структуру, придерживаясь требований по языку, Node.js 25 и архитектуре классов
