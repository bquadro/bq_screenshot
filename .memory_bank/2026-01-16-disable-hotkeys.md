# Отключение горячих клавиш 2026-01-16
- Удалил всю логику захвата горячих клавиш из `App.vue` (включая `hotkeys-js`, `nextTick`, временные состояния) и оставил простой обработчик `handleHotkeyCapture`, который просто выставляет сообщение `settings.hotkeysDisabled`.
- Добавил перевод для сообщения об отключённых горячих клавишах в `lang/ru.js` и `lang/en.js`.
- Это убирает зависания, связанные с `hotkeys-js`, и временно отключает функцию захвата, оставляя UI рабочим.
