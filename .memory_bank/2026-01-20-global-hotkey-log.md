Добавил лог в глобальный контроллер горячих клавиш:

- `GlobalHotkeyController` теперь печатает `Global hotkey triggered: <combo>` перед тем, как отправить `tray-action`, чтобы видеть в консоли, какая комбинация сработала.
