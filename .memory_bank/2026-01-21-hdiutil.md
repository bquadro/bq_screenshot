# DMG через hdiutil 2026-01-21

- удалил `electron-installer-dmg` и файл `app/create_dmg.js`, чтобы больше не использовать npm-обёртки от macos-alias и appdmg;
- `scripts/build_publish_mac.sh` теперь распаковывает первый `out/make/zip/darwin/...zip`, применяет `hdiutil create ... -srcfolder` и кладёт релизный `.dmg` в `out/make/dmg`, после чего запускает `npm run publish`; добавил проверки наличия `unzip` и `hdiutil` и учёл проброс `PRODUCT_NAME` из `package.json`;
- обновил `README.md` (теперь рассказывается о ZIP → hdiutil) и пересобрал `app/package-lock.json` после удаления зависимости.
