# Сохранение скриншотов 2026-01-16
- Изменил `ipcMain`-обработчик `save-screenshot` так, чтобы он больше не открывал диалог и сразу создавал файл `bq-screenshot-<ts>.png` в папке из `settings.storage.folder` (с падением на `~/Pictures/bq-screenshots`, если поле пустое).
- Добавил `mkdir` перед записью, чтобы нужная директория создавалась при первом сохранении, и возвращаю результат с `filePath`, чтобы UI мог показать путь и статус сохранения.
- Заодно удалил `dialog` из `main.js`; теперь путь настраивается в `SettingsStorage.settings.storage.folder` и читается при каждом сохранении скриншота.
