Реализовал экран настроек, включающий все функции из AGENTS.md (скриншоты, видео, редактор, трей, горячие клавиши, путь сохранения, подключение к Minio/S3 и выгрузку).
- Добавил в `main.js` хранение настроек в `settings.json` внутри `userData`, добавил IPC-обработчики `load-settings`/`save-settings` и гарантировал чтение/запись с дефолтами.
- Расширил `preload.js` API: теперь renderer получает функции загрузки/сохранения настроек вместе с функцией создания скриншота.
- Обновил `index.html`/`renderer.js`: сформировал раздел настроек с формой и полями для всех пунктов, подкрутил UI/статусы и реализовал привязку формы к API.
