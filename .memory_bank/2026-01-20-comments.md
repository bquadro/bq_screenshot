Добавил комментарии ко всем методам:

- Классы `SettingsStorage`, `IpcHandlers`, `SettingsService`, `LocalizationService`, `CaptureService` теперь подробно описывают назначение конструктора и каждого публичного метода.
- В `App.vue` задокументировал структуру формы, функции управления страницами, обработки действий и обновления состояний, а также обертку переводов.
- В `main.js` и `preload.js` объяснил, как создается окно, инициализируются IPC и осуществляется захват экрана; `npm run lint` по-прежнему просто выводит заглушку.
